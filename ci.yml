name: ci
on: [push, pull_request]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Backend
        run: |
          cd backend
          npm i
          npm run build
          npm test
      - name: Frontend
        run: |
          cd frontend
          npm i
          npm run build
